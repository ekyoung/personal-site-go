test:
  post:
   # Run the tests a second time with output in junit format.
   - go get -u github.com/jstemmer/go-junit-report
   - go test -v -race ./... | go-junit-report > $CIRCLE_TEST_REPORTS/report.xml